import { expect } from "chai";
import { isOddOrEven } from "../isOddOrEven.js";

describe("Test streamingServiceSelector", function () {
  describe("Test selectingContent", function () {
    it("", () => {
      expect(selectingContent('Movie', 'Netflix', )).to.be.undefined;
      expect().to.be.undefined;
      expect().to.be.undefined;
    });

    it("", () => {
      expect().to.equal();
      expect().to.equal();
    });

    it("", () => {
      expect().to.equal();
      expect().to.equal();
    });

    it("", () => {
      expect().to.equal();
      expect().to.equal();
      expect().to.equal();
    });
  });
  describe("Test availablePlatforms", function () {
    it("", () => {
      expect().to.be.undefined;
      expect().to.be.undefined;
      expect().to.be.undefined;
    });

    it("", () => {
      expect().to.equal();
      expect().to.equal();
    });

    it("", () => {
      expect().to.equal();
      expect().to.equal();
    });

    it("", () => {
      expect().to.equal();
      expect().to.equal();
      expect().to.equal();
    });
  });
});
describe("Test contentRating", function () {
  it("", () => {
    expect().to.be.undefined;
    expect().to.be.undefined;
    expect().to.be.undefined;
  });

  it("", () => {
    expect().to.equal();
    expect().to.equal();
  });

  it("", () => {
    expect().to.equal();
    expect().to.equal();
  });

  it("", () => {
    expect().to.equal();
    expect().to.equal();
    expect().to.equal();
  });
});
